{
  "name": "taskaws",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "check-types": "turbo check-types",
    "dev:web": "turbo -F web dev",
    "dev:server": "turbo -F server dev",
    "db:push": "turbo -F @taskaws/db db:push",
    "db:studio": "turbo -F @taskaws/db db:studio",
    "db:generate": "turbo -F @taskaws/db db:generate",
    "db:migrate": "turbo -F @taskaws/db db:migrate",
    "build:api": "turbo -F server build:api"
  },
  "dependencies": {
    "@taskaws/env": "workspace:*",
    "dotenv": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@taskaws/config": "workspace:*",
    "@types/node": "^22.13.14",
    "turbo": "^2.9.16",
    "typescript": "catalog:"
  },
  "packageManager": "pnpm@11.6.0"
}
